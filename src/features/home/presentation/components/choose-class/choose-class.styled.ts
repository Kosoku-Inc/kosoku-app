import styled from 'styled-components/native';

export const ChooseClassButtonsWrapper = styled.View`
    flex-direction: row;
`;

export const ChooseClassWrapper = styled.View`
    flex: 1;
    margin-vertical: ${(props) => props.theme.spacer * 2}px;
`;
